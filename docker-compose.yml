services:
  web:
    build: .
    command: >
      sh -c "pnpm dlx prisma generate && 
             pnpm dlx prisma db push && 
             pnpm dev"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

volumes:
  postgres_data:
